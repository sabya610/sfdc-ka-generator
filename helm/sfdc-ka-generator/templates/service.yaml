apiVersion: v1
kind: Service
metadata:
  name: {{ include "sfdc-ka-generator.fullname" . }}
  labels:
    {{- include "sfdc-ka-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sfdc-ka-generator.selectorLabels" . | nindent 4 }}
